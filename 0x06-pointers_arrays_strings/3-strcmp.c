#include "main.h"
#include<string.h>

/**
 * _strcmp - compare two string
 * @s1: string to be compared
 * @s2: string to be compared
 * Return: 0 - if match, less than 0 if s1 less than s2, > 0 if s1 greater s2
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
