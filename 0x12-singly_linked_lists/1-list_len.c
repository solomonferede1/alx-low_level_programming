#include "lists.h"
/**
 * list_len - return size of a linked list
 * @h: linked lists
 * Return: size of linked lists
 */
size_t list_len(const list_t *h)
{
        size_t count;
        size_t i;

            
}